#include<bits/stdc++.h>
using namespace std;

int main(){

    int punkajMark = 80, nomanMarks = 60;
    cout<<"less than operator "<<(punkajMark<nomanMarks)<<endl;  //0
    cout<<"greater than operator "<<(punkajMark>nomanMarks)<<endl; //1
    cout<<"less than or equal to operator "<<(punkajMark<=nomanMarks)<<endl; //0
    cout<<"greater than or equal to operator "<<(punkajMark>=nomanMarks)<<endl;  //1
    cout<<"not equal to operator "<<(punkajMark!=nomanMarks)<<endl;  //1
    cout<<"equal to equal operator "<<(punkajMark==nomanMarks)<<endl;  //0
}